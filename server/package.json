{
  "name": "you-could-be-mern-server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "NODE_ENV=test jest --verbose --watch --runInBand",
    "start:test": "NODE_ENV=test node index.js",
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development nodemon index.js",
    "build:ui": "rm -rf build && cd ../hkapp-client && npm run build --prod && cp -r build ../hkapp-server/",
    "deploy": "git push heroku master",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
    "logs:prod": "heroku logs --tail"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^9.0.2",
    "express": "^4.17.1",
    "express-async-errors": "^3.1.1",
    "jest": "^26.6.3",
    "mongoose": "^5.12.10"
  },
  "devDependencies": {
    "eslint": "^7.26.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
